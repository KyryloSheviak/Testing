
@model Testing.Domain.Models.TestsViewModel

@{
    ViewData["Title"] = "Home Page";
}

<h2>Тесты</h2>

@using (Html.BeginForm("Submit", "Home", FormMethod.Post, new { @name = "Form1" }))
{
    <div class="col-md-6">
        <label for="subject">Выберите предмет:</label>
        <select name="subject" id="subject" class="form-control">
            @foreach (var comp in Model.Subject)
            {
                <option value="@comp">@comp</option>
            }
        </select>
    </div>

    <div class="col-md-6">
        <label for="level">Выберите предмет:</label>
        <select name="level" id="level" class="form-control">
            @foreach (var lev in Model.Level)
            {
                <option value="@lev">@lev</option>
            }
        </select>
    </div>
    
    <div class="col-md-6">
        <br />
        <input type="submit" />
    </div>
    
}

<br />
<table class="table">
    @foreach (Testing.Domain.Entitys.Test p in Model.Test)
    {
        <tr>
            <td>@p.Subject</td>
            <td>@p.TimeToGo</td>
            <td>@p.CountQuestions</td>
            <td>@p.Сomplexity.Complication</td>
        </tr>
    }
</table>